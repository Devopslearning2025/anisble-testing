---
- name: Variables from vars file
  hosts: localhost
  vars_files:
    - vars.yaml
  tasks:
    - name: print infor vars files
      ansible.builtin.debug:
        msg: "Hi, I am {{Name}} and Learning {{Course}} with AWS for next {{Duration}}""




# - name: variables from files 
#   hosts: web
#   vars_files:
#   - vars.yaml
#   tasks:
#   - name: print override information 
#     ansible.builtin.debug:
#       msg: "Hi I am {{Name}} and learning {{Course}} course with duration is {{Duration}}"